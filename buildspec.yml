version: 0.2

phases:
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...........
  build:
    commands:
      - echo Build started on `date`
      - mvn install
  post_build:
    commands:
      - mvn mvn clean install

artifacts:
  files:
    - '**/springboot-curd-restful-webservices-0.0.1-SNAPSHOT.jar'
 
