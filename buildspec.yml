version: 0.2

phases:
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...........
  build:
    commands:
      - echo Build started on `date`
      - mvn install
  post_build:
    commands:
      - mvn clean install

artifacts:
  files:
    - target/springboot-crud-restful-webservices.jar
 
